#!/bin/bash
set -e

# Remove old server PID if it exists
rm -f tmp/pids/server.pid

# Optionally run migrations (skip errors if DB isn't up)
bundle exec rails db:migrate 2>/dev/null || echo "Skipping db:migrate"

# Start the dev server
exec "$@"